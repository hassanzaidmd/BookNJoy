# Server Configuration
PORT=5000

# MongoDB Database URI (Leave blank to use the in-memory fallback server)
MONGO_URI=mongodb://localhost:27017/sortmyscene

# JWT secret key for signature verification
JWT_SECRET=sort_my_scene_super_secret_token_key_change_me_in_production
